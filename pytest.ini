[pytest]

markers = 
    smoke: fast sanity checks that the device is reachable and in a known state
    regression: full regression suite covering firmware/config variants
    flaky: tests known to be environment-sensitive, candidates for rerun
testpaths = tests
addopts = --reruns 2 --reruns-delay 1